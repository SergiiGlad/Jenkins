#!groovy

library identifier: 'my-shared-library@master', retriever: modernSCM(github(repository: 'Jenkins', repoOwner: 'SergiiGlad'))

def lib = library('my-shared-library@master').com.sun

node{

  git credentialsId: 'github_git',
    url: 'https://github.com/sergiiglad/web-delivery.git'

  def yamlFilePathList = fileList('.')

  if ( yamlFilePathList ) println yamlFilePathList

  List<lib.SunDeploy> stages = [] 

  yamlFilePathList.each {
      stages.add( new lib.SunDeploy( it ) )
  }

  stages.each() {
    it.print()
  }

}